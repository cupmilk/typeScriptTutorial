"use strict";
var count = 0; //숫자
count += 1;
count = "문자열"; //에러발생
var text = "hello, ts";
var dome = true;
var numbers = [1, 2, 3]; //숫자 배열
var texts = ["h", "o"]; // 문자배열
texts.push(1); //'number' 형식의 인수는 'string' 형식의 매개 변수에 할당될 수 없습니다.
var knowUndefined = undefined;
